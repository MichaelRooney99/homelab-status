# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
proxy/.env

# Locally-generated snapshot history — the local dev fallback path for
# db.ts. In production this lives in a Docker named volume instead, not
# the repo. Not source, shouldn't be tracked, unlike incidents.json.
proxy/snapshots.db