node_modules
dist
.git
.gitignore
README.md

# Keep the client build context from pulling in the proxy's own
# node_modules/dist — proxy has its own Docker build context (./proxy)
proxy/node_modules
proxy/dist
proxy/.env
