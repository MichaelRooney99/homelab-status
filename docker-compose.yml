services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped

  proxy:
    build:
      context: ./proxy
      dockerfile: Dockerfile
    env_file:
      - ./proxy/.env
    ports:
      - "3001:3001"
    restart: unless-stopped